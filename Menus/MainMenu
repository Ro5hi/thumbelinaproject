public class Menu {
    int Start;
    int Gallery;
    int Settings;
    int Exit;

    void Start () {
        // start game
    }

    void Gallery () {
        // view gallery 
    }

    void Settings () {
        // change game settings
    }

    void Exit () {
        // exit game
    }
}

